const init = () => {
    const inputName = document.querySelector('input[name="UserName"]');
    const inputEmail = document.querySelector('input[name="UserEmail"]');
    const inputObject = document.querySelector('input[name="Object"]');
    const inputMessage = document.querySelector('input[name="Message"]');
    const submitButton = document.querySelector('.submitform');

    console.log('inputName, inputEmail, inputObject, inputMessage, submitButton');
}
